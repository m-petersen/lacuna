"""Preparation utilities for Lacuna workflows.

Subcommands exposed via the CLI:

- ``lacuna bidsify`` — convert a directory of NIfTI masks into BIDS layout.
- ``lacuna parcellate`` — reduce a whole-brain connectome to a parcellated
  N\u00d7N :class:`~lacuna.core.data_types.ConnectivityMatrix`.
"""
